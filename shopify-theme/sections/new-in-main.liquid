{%- liquid
  assign ni_titles = 'Tree of Life Jewellery Set Green,Luxury Soap Flower Bouquet,Zen Shower Steamers,Wooden Garden Mushroom,Love Healing Crystal Set,Set of 2 Witch Candles,Midnight Bloom Tulip Candle,Serenity Self-Care Kit' | split: ','
  assign ni_prices = '£12.00,£16.00,£8.00,£10.00,£9.99,£12.99,£9.99,£22.00' | split: ','
  assign ni_types  = 'Jewellery,Wellbeing,Wellbeing,Garden,Crystals,Candles,Candles,Wellbeing' | split: ','
  assign ni_colors = '#b8c9a9,#c0786a,#c9b8a8,#b8c9a9,#c0786a,#3d3d35,#3d3d35,#c9b8a8' | split: ','
-%}

<!-- Page Header -->
<section class="page-header">
  <div class="container">
    <h1>{{ section.settings.title }}</h1>
    <p>{{ section.settings.subtitle }}</p>
  </div>
</section>

<!-- New In Grid -->
<section class="section" style="padding-top:48px;">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">{{ section.settings.grid_title }}</h2>
      <p class="section-subtitle">{{ section.settings.grid_subtitle }}</p>
      <div class="section-divider"></div>
    </div>

    <div class="product-grid">
      {%- assign new_col = collections[section.settings.collection] -%}
      {%- assign rendered = 0 -%}

      {%- if new_col != blank -%}
        {%- for product in new_col.products limit: 8 -%}
          {%- render 'product-card', product: product -%}
          {%- assign rendered = rendered | plus: 1 -%}
        {%- endfor -%}
      {%- else -%}
        {%- for product in collections.all.products -%}
          {%- if product.tags contains 'new' or product.tags contains 'new-in' -%}
            {%- render 'product-card', product: product -%}
            {%- assign rendered = rendered | plus: 1 -%}
            {%- if rendered >= 8 -%}{%- break -%}{%- endif -%}
          {%- endif -%}
        {%- endfor -%}
      {%- endif -%}

      {%- assign needed = 8 | minus: rendered -%}
      {%- if needed > 0 -%}
        {%- for i in (1..needed) -%}
          {%- assign idx   = rendered | plus: forloop.index0 -%}
          {%- assign pt    = ni_titles[idx] | default: 'New Arrival' -%}
          {%- assign pp    = ni_prices[idx] | default: '£12.00' -%}
          {%- assign ptype = ni_types[idx]  | default: 'Home Décor' -%}
          {%- assign pclr  = ni_colors[idx] | default: '#e5ddd3' -%}
          <div class="product-card">
            <div class="product-card-image">
              <div style="position:absolute;inset:0;background:linear-gradient(160deg,{{ pclr }},#f8f5f0);display:flex;align-items:center;justify-content:center;">
                <svg xmlns="http://www.w3.org/2000/svg" width="44" height="44" fill="none" viewBox="0 0 24 24" stroke="rgba(255,255,255,0.35)" stroke-width="1">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
              </div>
              <span class="product-badge new">New</span>
              <div class="product-card-quick" style="cursor:default;">Add to Cart</div>
            </div>
            <div class="product-card-info">
              <p style="font-size:0.68rem;text-transform:uppercase;letter-spacing:1.5px;color:var(--text-light);margin-bottom:6px;">{{ ptype }}</p>
              <h3 class="product-card-title">{{ pt }}</h3>
              <div class="product-card-price"><span class="price">{{ pp }}</span></div>
            </div>
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>

    <div class="section-cta">
      <a href="{{ routes.all_products_collection_url }}" class="btn btn-outline">View All Products</a>
    </div>
  </div>
</section>

<!-- Featured Banner -->
<section class="featured-banner">
  <div class="container">
    <div class="featured-banner-inner">
      <div class="featured-banner-content">
        <span class="banner-label">{{ section.settings.banner_label }}</span>
        <h2>{{ section.settings.banner_heading }}<br><em>{{ section.settings.banner_heading2 }}</em></h2>
        <p>{{ section.settings.banner_text }}</p>
        <a href="{{ section.settings.banner_cta_url | default: routes.all_products_collection_url }}" class="btn btn-primary">
          {{ section.settings.banner_cta }}
        </a>
      </div>
      <div class="featured-banner-image">
        {%- if section.settings.banner_image -%}
          <img src="{{ section.settings.banner_image | image_url: width: 800 }}" alt="{{ section.settings.banner_heading | escape }}" loading="lazy">
        {%- else -%}
          <div class="featured-banner-placeholder">
            <div class="featured-banner-placeholder-inner" style="display:flex;flex-direction:column;align-items:center;justify-content:center;gap:16px;">
              <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;width:200px;">
                {%- assign demo_colors = '#b8c9a9,#c9b8a8,#b8bfc9,#d4c4b0' | split: ',' -%}
                {%- assign demo_labels = 'Candles,Crystals,Home Décor,Self-Care' | split: ',' -%}
                {%- for j in (0..3) -%}
                  <div style="background:{{ demo_colors[j] }};border-radius:8px;height:80px;display:flex;align-items:center;justify-content:center;font-size:0.7rem;color:white;font-weight:600;text-align:center;padding:4px;">{{ demo_labels[j] }}</div>
                {%- endfor -%}
              </div>
              <p style="color:var(--text-light);font-style:italic;font-size:0.85rem;">Add a banner image</p>
            </div>
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

<!-- Newsletter -->
<section class="newsletter">
  <div class="container">
    <div class="newsletter-icon">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5"><path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
    </div>
    <h2>{{ section.settings.nl_title }}</h2>
    <p>{{ section.settings.nl_text }}</p>
    {%- form 'customer', id: 'new-in-newsletter' -%}
      <input type="hidden" name="contact[tags]" value="newsletter">
      <div class="newsletter-form">
        <input type="email" name="contact[email]" placeholder="{{ section.settings.nl_placeholder }}" required aria-label="Email"
               style="color:#3d3d35 !important;-webkit-text-fill-color:#3d3d35 !important;background:transparent;border:none;outline:none;flex:1;padding:12px 8px;font-family:inherit;font-size:1rem;">
        <button type="submit" class="btn btn-light">{{ section.settings.nl_btn }}</button>
      </div>
    {%- endform -%}
    <p class="newsletter-note">No spam, ever. Unsubscribe any time.</p>
  </div>
</section>

{% schema %}
{
  "name": "New In",
  "settings": [
    { "type": "text",         "id": "title",          "label": "Page title",       "default": "New In" },
    { "type": "text",         "id": "subtitle",       "label": "Page subtitle",    "default": "Fresh arrivals to make your home feel even more special" },
    { "type": "text",         "id": "grid_title",     "label": "Grid title",       "default": "Just Landed" },
    { "type": "text",         "id": "grid_subtitle",  "label": "Grid subtitle",    "default": "The newest pieces added to our cosy collection" },
    { "type": "collection",   "id": "collection",     "label": "New In collection" },
    { "type": "text",         "id": "banner_label",   "label": "Banner label",     "default": "Just Arrived" },
    { "type": "text",         "id": "banner_heading", "label": "Banner heading",   "default": "Beautiful New" },
    { "type": "text",         "id": "banner_heading2","label": "Banner heading 2", "default": "Arrivals" },
    { "type": "textarea",     "id": "banner_text",    "label": "Banner text",      "default": "From healing crystals and hand-poured candles to spiritual gifts and cosy home accessories — our latest arrivals are thoughtfully chosen for homes full of light and wonder." },
    { "type": "text",         "id": "banner_cta",     "label": "Banner button",    "default": "Shop All New In" },
    { "type": "url",          "id": "banner_cta_url", "label": "Banner button URL" },
    { "type": "image_picker", "id": "banner_image",   "label": "Banner image" },
    { "type": "text",         "id": "nl_title",       "label": "Newsletter title", "default": "Never Miss a New Arrival" },
    { "type": "text",         "id": "nl_text",        "label": "Newsletter text",  "default": "Subscribe to be the first to see our latest cosy finds." },
    { "type": "text",         "id": "nl_placeholder", "label": "Email placeholder","default": "Your email address" },
    { "type": "text",         "id": "nl_btn",         "label": "Subscribe button", "default": "Subscribe" }
  ],
  "presets": [{ "name": "New In" }]
}
{% endschema %}
