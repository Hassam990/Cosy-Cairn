<section class="section section--warm">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">{{ section.settings.title }}</h2>
      <p class="section-subtitle">{{ section.settings.subtitle }}</p>
      <div class="section-divider"></div>
    </div>

    {%- assign src_collection = collections[section.settings.collection] -%}
    {%- assign product_list = src_collection.products | default: collections.all.products -%}

    {%- if product_list.size > 0 -%}
      <div class="product-grid">
        {%- for product in product_list limit: section.settings.products_count -%}
          {%- render 'product-card', product: product -%}
        {%- endfor -%}
      </div>
      <div class="section-cta">
        <a href="{{ section.settings.cta_url | default: routes.all_products_collection_url }}" class="btn btn-outline">
          {{ section.settings.cta_label }}
        </a>
      </div>
    {%- else -%}
      <div class="empty-state">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1"><path stroke-linecap="round" stroke-linejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>
        <h3>Products coming soon</h3>
        <p>Import your products using the CSV file and they'll appear here automatically.</p>
        <a href="{{ routes.all_products_collection_url }}" class="btn btn-primary">Browse Shop</a>
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Featured Products",
  "settings": [
    { "type": "text",       "id": "title",          "label": "Title",              "default": "Shop Our Favourites" },
    { "type": "text",       "id": "subtitle",       "label": "Subtitle",           "default": "Hand-picked pieces we think you'll love" },
    { "type": "collection", "id": "collection",     "label": "Collection",         "info": "Leave blank to show all products" },
    { "type": "range",      "id": "products_count", "label": "Number of products", "min": 4, "max": 12, "step": 2, "default": 8 },
    { "type": "text",       "id": "cta_label",      "label": "Button label",       "default": "View All Products" },
    { "type": "url",        "id": "cta_url",        "label": "Button URL" }
  ],
  "presets": [{ "name": "Featured Products" }]
}
{% endschema %}
